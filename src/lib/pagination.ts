// How many media cards to render per batch. Shared by the library grid's
// infinite scroll (network-paginated) and the board's per-column windowing
// (client-side reveal of already-loaded data) so both views feel consistent.
export const MEDIA_PAGE_SIZE = 40;
